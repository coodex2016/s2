const headers = {
    'search' : true,
    'github': true
}

const theme = {
    'default': 'default',
    'merge': true,
    'options' :{}
}

export const globalData = {
    'name': 'demo application',
    'app': {
        'title': 'title',
    },
    headers,
    theme
};
